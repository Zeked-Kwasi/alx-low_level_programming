#include <stdio.h>
#include <stdlib.h>

/**
 * main - code entry
 * @argc: parameter counter
 * @argv: array pointer
 *
 * Return: code status
 */

int main(int argc, char **argv)
{
if (argc > 0)
	printf("%s\n", argv[0]);
return (0);
}
